# Docker service port configuration
# This file defines the ports used by Docker services
# Modify these values if you need to change the default ports

# Web server (Nginx)
HTTP_PORT=80
HTTPS_PORT=443

# Database (MySQL)
MYSQL_PORT=3306

# PHP-FPM
PHP_PORT=9000

# Vite development server
VITE_PORT=5173

# Additional services
# Add any other service ports here
# REDIS_PORT=6379
# MAILHOG_PORT=8025
